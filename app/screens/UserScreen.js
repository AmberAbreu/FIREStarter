import React from 'react'
import { StyleSheet, Text, SafeAreaView } from 'react-native'

const UserScreen = () => {
  return (
    <SafeAreaView>
      <Text>Periodt pooh</Text>
    </SafeAreaView>
  )
}

export default UserScreen

const styles = StyleSheet.create({})
